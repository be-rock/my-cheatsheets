# useful repr 
return f"{self.__class__.__name__}({', '.join([f'{k}={v}' for k, v in self.__dict__.items()])})"

# timezone aware utc timestamp
from datetime import datetime
datetime.now(timezone.utc).isoformat()
'2015-01-27T06:59:17.125448+00:00'
